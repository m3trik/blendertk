# !/usr/bin/python
# coding=utf-8
"""Channels — Switchboard UI for inspecting and editing Blender object channels.

Docstring-only (blendertk convention): the ``Channels`` engine and ``ChannelsSlots`` are imported
directly from their modules. Keeping this empty avoids pulling Qt (via the slots) at package-import
time, so the engine resolves under headless ``--background``.
"""
