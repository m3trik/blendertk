# !/usr/bin/python
# coding=utf-8
"""Naming tool — Blender port of mayatk's ``edit_utils.naming`` subpackage.

Engine (``Naming``) + co-located Switchboard panel (``NamingSlots`` + ``naming.ui``), mirroring the
mayatk package layout. Subpackage ``__init__`` is docstring-only (no import side effects).
"""
