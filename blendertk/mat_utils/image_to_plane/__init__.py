# !/usr/bin/python
# coding=utf-8
"""Image-to-Plane tool — Blender port of mayatk's ``mat_utils.image_to_plane`` subpackage.

Engine (``ImageToPlane``) + co-located Switchboard panel (``ImageToPlaneSlots`` + ``image_to_plane.ui``),
mirroring the mayatk package layout. Subpackage ``__init__`` is docstring-only (no import side effects).
"""
