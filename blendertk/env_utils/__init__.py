# !/usr/bin/python
# coding=utf-8
"""Environment / scene-file utilities — Blender library-linking (reference) helpers,
mirroring mayatk's ``env_utils`` home for the Reference Manager tool.

Public surface is registered via the root ``DEFAULT_INCLUDE`` + ``bootstrap_package`` — this
module file is a docstring-only marker (mirrors the mayatk subpackage convention).
"""
